# silent-failure-demo
A booking agent where tools return 200 OK but deliver bad data. Instrumented with Progress Observability to show how traces catch failures that traditional monitoring misses.  Run this 20-30 times to generate traces with various failure patterns, then explore them at https://observability.progress.com
